// After document has loaded
window.addEventListener("DOMContentLoaded", event => {
    setTimeout(() => {
        document.getElementById('splash').classList.toggle('d-none');

    }, 2000);
});

// notification example
